import { useAuth } from "@/_core/hooks/useAuth";
import { Button } from "@/components/ui/button";
import { Loader2 } from "lucide-react";
import { getLoginUrl } from "@/const";
import { useLocation } from "wouter";

export default function Home() {
  const { user, loading, isAuthenticated } = useAuth();
  const [, setLocation] = useLocation();
  const loginUrl = getLoginUrl();

  if (loading) {
    return (
      <div className="min-h-screen flex items-center justify-center bg-background">
        <Loader2 className="w-8 h-8 animate-spin text-accent" />
      </div>
    );
  }

  if (isAuthenticated) {
    setLocation("/dashboard");
    return null;
  }

  return (
    <div className="min-h-screen flex flex-col bg-background">
      {/* Navigation */}
      <nav className="border-b border-border bg-card">
        <div className="container py-4 flex items-center justify-between">
          <div className="flex items-center gap-2">
            <div className="w-8 h-8 rounded-lg bg-accent flex items-center justify-center">
              <span className="text-accent-foreground font-bold text-sm">PA</span>
            </div>
            <span className="text-lg font-bold text-foreground">PetAlert</span>
          </div>
          <a href={loginUrl} className="text-sm text-muted-foreground hover:text-foreground transition-smooth">
            Sign In
          </a>
        </div>
      </nav>

      {/* Hero Section */}
      <section className="flex-1 flex items-center">
        <div className="container py-24 md:py-32">
          <div className="max-w-3xl">
            <h1 className="text-5xl md:text-6xl font-bold text-foreground mb-6 leading-tight">
              Monitor Your Pets with <span className="text-accent">Real-Time Alerts</span>
            </h1>
            <p className="text-xl text-muted-foreground mb-8 leading-relaxed">
              PetAlert Warning System empowers pet owners to stay connected with their beloved companions. Receive instant notifications when important thresholds are triggered, ensuring your pets' safety and well-being at all times.
            </p>
            <div className="flex flex-col sm:flex-row gap-4">
              <a
                href={loginUrl}
                className="inline-flex items-center justify-center px-8 py-3 rounded-lg bg-accent text-accent-foreground font-semibold hover:bg-accent/90 transition-smooth"
              >
                Get Started
              </a>
              <button className="inline-flex items-center justify-center px-8 py-3 rounded-lg border border-border text-foreground font-semibold hover:bg-muted transition-smooth">
                Learn More
              </button>
            </div>
          </div>
        </div>
      </section>

      {/* Features Section */}
      <section className="bg-card border-t border-border py-24">
        <div className="container">
          <h2 className="text-3xl md:text-4xl font-bold text-foreground mb-16 text-center">
            Powerful Features for Pet Owners
          </h2>
          <div className="grid md:grid-cols-3 gap-8">
            {[
              {
                title: "Real-Time Monitoring",
                description: "Track your pet's vital metrics and environmental conditions with instant updates.",
                icon: "📊",
              },
              {
                title: "Custom Alerts",
                description: "Set personalized warning thresholds for each pet and receive notifications when triggered.",
                icon: "🔔",
              },
              {
                title: "Alert History",
                description: "Review past alerts and events to identify patterns and trends in your pet's behavior.",
                icon: "📋",
              },
            ].map((feature, idx) => (
              <div key={idx} className="p-6 rounded-lg border border-border bg-background hover:shadow-premium transition-smooth">
                <div className="text-4xl mb-4">{feature.icon}</div>
                <h3 className="text-xl font-semibold text-foreground mb-3">{feature.title}</h3>
                <p className="text-muted-foreground">{feature.description}</p>
              </div>
            ))}
          </div>
        </div>
      </section>

      {/* CTA Section */}
      <section className="py-24 bg-background">
        <div className="container text-center">
          <h2 className="text-3xl md:text-4xl font-bold text-foreground mb-6">
            Ready to Protect Your Pets?
          </h2>
          <p className="text-lg text-muted-foreground mb-8 max-w-2xl mx-auto">
            Join thousands of pet owners who trust PetAlert Warning System to keep their companions safe.
          </p>
          <a
            href={loginUrl}
            className="inline-flex items-center justify-center px-8 py-3 rounded-lg bg-accent text-accent-foreground font-semibold hover:bg-accent/90 transition-smooth"
          >
            Start Your Free Trial
          </a>
        </div>
      </section>

      {/* Footer */}
      <footer className="border-t border-border bg-card py-8">
        <div className="container text-center text-sm text-muted-foreground">
          <p>&copy; 2026 PetAlert Warning System. All rights reserved.</p>
        </div>
      </footer>
    </div>
  );
}
